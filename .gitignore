# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# TokenWatt local data (ledger, calibration profiles, rate cache)
*.sqlite
*.sqlite-*
.tokenwatt/

# leak-scan personal deny-list — literal names/employer must never be committed
.githooks/leak-patterns.local

# macOS
.DS_Store
